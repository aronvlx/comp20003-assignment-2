Generative AI was used to debug and create testing samples between the creation of individual .c files

- AI was used to generate a sample test immediately after creating data.c, data.h, list.c, list.h to ensure matching with correct output formatting according to the outputs found in the 'matching_results' folder provided.

- AI was used again after the creation of csv.c, csv.h to create a sample test to test and debug the splitting and reading logic of those files on a .csv input and checking that the output into a .txt file was correct and well formatted.

- Once again used after modifications and additions to bit.c, bit.h, data.c, data.h to create tests for the bit comparison logic and record reading logic

- Used after the creation of search.c, search.h to test dictionary searching by key logic.

- Used to research amortization deeper 
